/** Router location.state shapes used for one-shot flash messages */
export type ListFlashLocationState = {
  flashMessage?: string
}

export type DetailCreatedLocationState = {
  createdFlash?: boolean
}
